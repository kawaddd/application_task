<div class="container">
  <div class="row">
  <div class="col-md-1"></div>
  <h3>注文履歴一覧</h3>
  </div>
  
  <div class="row">
    <table class="col-12">
      <thead>
        <tr>
          <th>購入日時</th>
          <th>購入者</th>
          <th>注文個数</th>
          <th>注文ステータス</th>
        </tr>
      </thead>
      <tbody>
        <% @orders.each do |order| %>
        <tr>
          <td><%= link_to order.created_at, admin_order_path(order)  %></td>
          <td><%= order.customer.last_name + order.customer.first_name %></td>
          <td>
            <%# sum = 0%>
            <%# order.order_details.each do |detail|%>
            <%# sum = sum + detail.item.price *detail.amount %>
            <%#end%>
            <%#= sum %>
          </td>
          <td><%= order.status %></td>
        </tr>
        <% end %>
      </tbody>
    </table>
  </div>
</div>
<%= paginate @orders %>